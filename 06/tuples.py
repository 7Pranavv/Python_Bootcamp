a=(3,2,22,13)
print(a)
print(a[2])

# Tuples are immutable, so we cannot change their elements
b=(3,) # This is a tuple with one element
print(b) # To create a tuple with one element, we need to add a comma after the element  